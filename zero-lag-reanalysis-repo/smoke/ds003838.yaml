schema_version: 1
dataset_id: ds003838
role: primary

# Smoke null battery; production default is 1000 (DEFAULT_N_SURROGATES).
n_surrogates: 20

paths:
  raw_root: ../../data/raw/ds003838
  output_subdir: smoke/ds003838

selection:
  subjects:
    - sub-032
    - sub-033
    - sub-034
  tasks:
    - rest
    - memory
  conditions:
    - rest
    - memory
  sessions:
    - single

# HIIT confirmatory smoke (M13b) — PPG / photosensor primary.
# Pairing: PRE rest↔Tetris and POST rest↔Tetris within each PH/PS session.
# Companion files: beats.yaml (exploratory Stage 1b peaks), verification.json (stop rules).
# C0 runs the raw-data audit internally — do not require temporal_coupling --stage 0.
schema_version: 1
dataset_id: hiit
role: sensitivity

paths:
  raw_root: ../../../data/raw/HIIT
  output_subdir: smoke/hiit_m13b

selection:
  subjects:
    - "01"
    - "02"
    - "03"
  tasks:
    - rest
    - tetris
  conditions:
    - ph_pre_rest
    - ph_pre_tetris
    - ps_pre_rest
    - ps_pre_tetris
    - ph_post_rest
    - ph_post_tetris
    - ps_post_rest
    - ps_post_tetris
  sessions:
    - ph
    - ps

# Confirmatory zero-lag analysis — ds004582 full cohort (task FF).
schema_version: 1
dataset_id: ds004582
role: sensitivity

paths:
  raw_root: ../../data/raw/ds004582
  output_subdir: sensitivity/ds004582

selection:
  subjects: []
  tasks:
    - ff
  conditions:
    - ff
  sessions:
    - "01"

protocol:
  participant_id_from: observation_id
  session_id_from: session_label
  cardiac_event_type: r_peak

normalization:
  condition_semantics:
    ff: { state: state_low, time: time_na, session: "01" }

capabilities:
  has_eeg: true
  has_hr: true
  cardiac_modality: ecg
  has_ecg_r_peaks: true
  has_ppg_peaks: false
  has_low_high_task_pair: false
  has_pre_post_pair: false
  has_behavior: false
  supports_d180: true
  supports_d240: true
  supports_topography: true
  supports_gamma: true
  sensitivity_only: true
  has_artifact_controls: true
